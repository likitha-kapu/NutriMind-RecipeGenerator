const SearchBar = () => {
  return (
    <div className="search-bar">
      <input placeholder="Search recipes by name, ingredient, or type..." />
      <button>Search</button>
    </div>
  );
};

export default SearchBar;
